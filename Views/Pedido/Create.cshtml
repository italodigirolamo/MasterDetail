@model MasterDetail.Models.Pedido

@{
    //ViewData["Title"] = "Create";
}

@* @{
    for (var i = 0; i<ViewBag.Productos.Count; i++)
    {
        var productos = ViewBag.Productos[i];
    }
 
} *@

<h1>Create</h1>

<h4>Pedido</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Fecha" class="control-label"></label>
                <input asp-for="Fecha" class="form-control" />
                <span asp-validation-for="Fecha" class="text-danger"></span>
            </div>

            @* Seleccionar Datos Relacionados CLIENTES *@
            <div class="form-group">
                <label asp-for="ClienteId" class="control-label"></label>
                <select asp-for="ClienteId" class="form-control" asp-items="new SelectList(ViewBag.Clientes, 
                    nameof(Cliente.ClienteId), nameof(Cliente.Nombre)) " >

                    <option value="">---Seleccione un Cliente----</option>
                </select>
                @* <select asp-for="ClienteId" class ="form-control" asp-items="ViewBag.ClienteId"></select> *@
            </div>

            @* Seleccionar Datos Relacionados PRODUCTOS *@
            <div class="form-group">
                @* <label asp-for="ClienteId" class="control-label"></label> *@
@*                 <select asp-for="ClienteId" class="form-control" asp-items="new SelectList(ViewBag.Clientes,
                    nameof(Cliente.ClienteId), nameof(Cliente.Nombre)) ">
                    <option value="">---Seleccionar un Cliente----</option>
                </select> *@

                <select  class="form-control" asp-items="new SelectList(ViewBag.Productos, 
                    nameof(Producto.ProductoId), nameof(Producto.Nombre)) ">

                    <option value="">---Seleccione un Producto----</option>
                </select>

                @* <select asp-for="ClienteId" class ="form-control" asp-items="ViewBag.ClienteId"></select> *@
            </div>


            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
